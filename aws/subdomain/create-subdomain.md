# Create Sub-Domain in AWS

- Create A record pointing the EC2 instance IP
- Create Directory under html
- Add virtual host in /etc/php.ini
- Add directory permission in /etc/php.ini
- Restart httpd server 
    sudo service httpd restart

